<Window x:Class="SampleWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SampleWpf"
        mc:Ignorable="d"
        Title="Sample: Shop Characteristics Demo" Height="620" Width="980">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock FontSize="20"
                   FontWeight="SemiBold"
                   Text="Пример: как добавить характеристики в карточке товара (оставляя остальное как есть)"/>

        <Border Grid.Row="1" Margin="0,12,0,0" BorderBrush="#DDD" BorderThickness="1" CornerRadius="8" Padding="12">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="ItemsData">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="220" Background="White" Margin="8" Padding="8" CornerRadius="8" BorderBrush="#EEE" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="{Binding name, StringFormat=Название: {0}}" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding quantity, StringFormat=На складе: {0}}"/>
                                    <TextBlock Text="{Binding price, StringFormat=Цена: {0}}"/>
                                    <TextBlock Text="{Binding category_name, StringFormat=Категория: {0}}"/>
                                    <Button Margin="0,10,0,0"
                                            Height="30"
                                            Click="Characteristick_Click"
                                            Content="Характеристики"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
